[server]
addr = "0.0.0.0:3000"
protocol = "http"

[database]
database = "axum-template"
hostname = "127.0.0.1"
username = "postgres"
password = "123456"

[logger]
logo = "[AXUM]"
path = "logs"       # 日志路径
name = "%y%m%d.log" # 日志文件
file = true         # 写入文件
color = true        # 是否染色
stdout = true       # 写入终端
#delete = 1         # 定期删除(天)
time = "%y-%m-%d %H:%M:%S%.3f" # 时间显示格式

[jwt]
secret = "秘钥"
duration = 1296000 # 半个月
